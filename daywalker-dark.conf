# Daywalker Dark Theme
# Wrapper for tmux-dark-notify compatibility
# This file is sourced by tmux, so we calculate the plugin path dynamically

run-shell 'PLUGIN_DIR="$(dirname "$(tmux display -p "#{@dark-notify-theme-path-dark}")")"; "${PLUGIN_DIR}/scripts/daywalker.sh" dark'
