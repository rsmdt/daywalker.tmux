# Daywalker Light Theme
# Wrapper for tmux-dark-notify compatibility
# This file is sourced by tmux, so we calculate the plugin path dynamically

run-shell 'PLUGIN_DIR="$(dirname "$(tmux display -p "#{@dark-notify-theme-path-light}")")"; "${PLUGIN_DIR}/scripts/daywalker.sh" light'
